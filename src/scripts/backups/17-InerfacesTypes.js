"use strict";
/* let bandera: string = "true";

let edad: number;
edad = 3;

let otraVariable = 2;

let arr: (number | string)[] = [1,2,3, "1","2","3"];

console.log("miau");

interface ICalculator {
    suma(num1: number, num2: number): number;
    restar(num1: number, num2: number): number;
}

class Calculator implements ICalculator {
    constructor() {}

    restar(num1: number, num2: number): number {
        return num1 - num2;
    }

    suma(num1: number, num2: number): number {
        return num1 + num2;
    }
}

const calculadora = new Calculator();

console.log(calculadora.suma(3, 4));
console.log(calculadora.restar(3, 4));
 */ 
